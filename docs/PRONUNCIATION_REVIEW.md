# 誤読修正の記録 — 2026-09-08

対象：プロンプトキャッシュとPiのRemotion完全版。保存済み編集台本と元の動画用台本の文言・読み・話者・演出が一致することを確認し、2つのセリフの `speech` と `expectedKana` のみを改訂した。字幕・数値・章立て・出典は維持。元の台本・動画は保持する。

| 場面・セリフ | 字幕 | 元のVOICEVOX解析 | 修正後 |
|---|---|---|---|
| 4・4 | その理解で十分。… | ソノリカイデジュップン… | ソノリカイデジュウブン… |
| 5・6 | …一致で十分、… | …イッチデジュップン… | …イッチデジュウブン… |

VOICEVOX 0.25.2の実際の `audio_query` で誤読を再現した。両方とも「足りている」という意味なので、文脈を確認して `speech` の十分をじゅうぶんに補正。セリフ全体のカナを照合した。照合結果は出力先の `pronunciation-report.json` に残す。全文の聴感レビューは未実施であり、この確認は発音の元となるモーラ列の検査。

新しい完全版は `output/prompt-caching-remotion-full-20260908-v3/demo.mp4`。音声実測の完成尺は1202.875秒（20分2.875秒）。旧版から0.125秒増加。新しい実測音声に字幕・章・全30場面のアニメーションを再同期する。

改訂台本は `output/prompt-caching-reading-revision.json`、元の編集内容を保持した改訂用コピーは `output/prompt-caching-reading-outline.json`。後者は元プロジェクトのIDを保持したスナップショットであり、workspaceの元ファイルへ書き戻さない。新規読み込みには前者の台本JSONを使う。

## 今後の制作

[台本形式と照合方法](DIALOGUE_BOARD.md#文脈に応じた読みと合成前の照合)。この会話のAIが文脈から読みを指定する。ローカルアプリ内に文脈判定LLMを組み込んだわけではない。エンジンの自動読みを確認せず期待値にコピーしない。辞書の一律置換に頼らず、全文一致で違いを検出する。限定した候補語以外は自動検出されないため、数字・固有名詞も台本作成時に確認する。

検証：Python 33件、エディターの画面操作16件が成功。キャッシュ済みでも読みが違えば合成を停止する回帰テストを含む。修正セリフを本編の音声トラックから切り出した `readings.html` を同じ出力先に用意した。

最終検証：2026-09-08T08:33:44.422362+00:00。MP4 SHA-256 `7457e0c403d4fcc13383c8c0bc85bc490b091d468e6bcc67515acae661d0b4dc`。28,869フレーム・180字幕・10章・音声を全編デコードし、時刻を検証済み。音声のセリフ単位のハッシュ比較では、変更は4場面4セリフ目と5場面6セリフ目だけで、残り178セリフは同一。章ファイルの時刻表記は既存検証に合わせて時:分:秒へ修正した。
