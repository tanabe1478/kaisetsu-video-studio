## 01 エージェントの全体像 | 返事をするだけでは終わらない
R 今日は、AIエージェントの中身をコードから追っていくのね。
M 題材はpiのagent-coreだ。会話、ツール実行、次の判断をつなぐ仕組みを見るぜ。
R 普通にモデルへ質問するのと、何が違うのかしら。
M 例えば、2つのファイルを読んで違いを説明して、と頼んだ場面を考えよう。
M モデルが読み取りを要求し、プログラムが実行して、結果をモデルへ返す。
R 返ってきた結果を見てから、最終的な説明を作るのね。
M そうだ。この往復を管理するのが重要だ。モデル自身がファイルを直接読むわけではない。
M 黒板の点は処理の流れを表す。今回のファイル名や依頼は、理解のための例だぜ。

## 01 エージェントの全体像 | どの層を読んでいるのか
R agent-coreだけで、画面もモデルも全部そろうの？
M まず層を分けよう。pi-aiはモデルとの通信や、共通のメッセージ形式を受け持つ。
M agent-coreは、その上で会話の状態やツール実行の繰り返しを組み立てる。
R さらに上に、利用者が操作するアプリケーションが載るわけね。
M そうだ。画面に何を表示するか、どのツールを渡すかは、組み込む側の設計になる。
M 今回はコミットを固定して調べた。パッケージのバージョンは0.85.1だ。
R あとからリポジトリが変わっても、動画の根拠をたどれるのね。
M 古い記事の説明と混ぜず、この版に実際にある入口と処理を見ていこう。

## 01 エージェントの全体像 | 基本ループと実行基盤
R 入口のファイルを見ると、思ったよりいろいろ公開されているわね。
M 基本のAgentに加えて、AgentHarnessという上位の実行基盤も入っている。
M まず小さいAgentで往復を理解し、あとから保存やセッションの仕組みを重ねよう。
R 最初から全部の型を読もうとすると、目的を見失いそうだわ。
M だから、入力がどこに入り、何が更新され、いつ次の処理へ進むかを追うんだ。
M 前半は基本ループ、後半は並列処理と追加指示、最後に実行基盤を扱うぜ。
R ファイルの一覧を暗記するより、依頼が終わるまでの道筋をつかむのね。
M 黒板も同じ順で広げる。小さな往復を、大きなアプリの中に位置づけよう。

## 02 入力と状態 | Agentに渡すもの
R では最初のAgentには、何を渡せば動き始めるの？
M モデルの情報やシステム指示、利用できるツール、それに応答を取得する関数だ。
M READMEでは、モデル管理オブジェクトのstreamSimpleをstreamFnに渡している。
R 通信処理が、Agentの中へ固定で埋め込まれているわけではないのね。
M その通り。応答を返す口を差し替えられるので、テスト用の応答も渡せる。
M ツールが空なら、会話だけの使い方もできる。必要な能力を組み込む側が選ぶんだ。
R ここで名前を登録しただけでは、モデルの判断まで決まるわけではないのね。
M 実行可能な選択肢を渡す段階だ。どれを呼ぶかは、その後の応答に現れるぜ。

## 02 入力と状態 | promptから履歴へ
R 利用者の文章は、どんな形で中へ入っていくのかしら。
M promptへ文字列を渡すと、ユーザーメッセージの内容と時刻を持つ形に整える。
M すでにメッセージの形なら、その入力を使う入口も用意されている。
R 単なる文字列の配列ではなく、役割を持った記録になるのね。
M そして現在のメッセージとツールの配列をコピーし、ループ用の文脈を作る。
M 実行が始まると、応答中かどうか、途中のメッセージなどの状態も更新される。
R 画面に出す途中経過と、確定した会話の履歴は分けて考えられそうね。
M そうだ。後で見るイベントが、その更新と画面への通知をつないでいるんだぜ。

## 02 入力と状態 | 同じAgentへ重ねて頼むと
R まだ返答中なのに、もう一度promptを呼んだらどうなるの？
M 基本のAgentでは、実行中の新しいpromptはエラーになる。二重起動を防ぐんだ。
R じゃあ、利用者が途中で補足したいときは困らない？
M そのためにsteerとfollowUpがある。今の実行へ指示を届ける専用の待ち行列だ。
M まずは、普通の依頼の開始と、実行中の追加指示が別の入口だと覚えよう。
R ボタンを連打したら、同じ履歴で勝手に2本の処理が走る設計ではないのね。
M 終了を待つにはwaitForIdleが使える。終了イベントの通知処理が済むのも待つ。
M 履歴を消すresetも実行中は拒否する。状態を変えるタイミングが大切なんだ。

## 03 モデルへ届くまで | 保存する文脈と送る文脈
R アプリの履歴は、そのまま全部モデルへ送られるのかしら。
M 必ずしもそうではない。送信前には、文脈を加工する段階と形式を変える段階がある。
M 先にtransformContext、その後でconvertToLlmという順に処理される。
R 例えば、画面だけに必要なお知らせまで、モデルに見せる必要はないものね。
M そうだ。独自のメッセージを変換したり、送信対象から外したりできる。
M 前段では、古いメッセージを減らすなど、文脈の量を調整する使い方も想定されている。
R 黒板では履歴のカードが、選別されてからモデル用の箱へ移っているわね。
M 送信用の文脈を変えることと、保存済みの履歴を永久に削除することは分けよう。

## 03 モデルへ届くまで | メッセージを変換する責任
R 独自の通知を足すときは、型を増やすだけで十分なの？
M モデルが理解できる役割へ変換するか、除外するかも決める必要がある。
M 基本形はuser、assistant、toolResultだ。アプリ独自の役割は変換の対象になる。
R 入力の文章、モデルの応答、ツールの結果を混ぜないための区別なのね。
M 例えば処理中のお知らせは画面に残しても、推論に不要なら送らない設計にできる。
M ただし、変換でツールの要求と結果の対応を壊してはいけない。
R 結果だけ取り除いたら、モデルからは実行が終わったのか分からなくなるわ。
M 文脈の節約は、意味のつながりも守る作業だ。単に先頭から削ればよいわけではないぜ。

## 03 モデルへ届くまで | 応答は少しずつ届く
R モデルの返事は、全部完成してから1回だけ返ってくるの？
M ストリームでは途中の更新が届く。開始、文字の追加、完了を順に扱うんだ。
M ループはそれをmessage_start、message_update、message_endへつなげる。
R 画面で文字が少しずつ増えていくのは、この更新を表示しているのね。
M 文字だけでなく、思考やツール呼び出しの更新も、イベントの種類として扱っている。
M ただし、表示方法はアプリ側の仕事だ。内部イベントをすべて利用者へ出す必要はない。
R 途中の応答を確定した返事として保存しないことも、重要そうだわ。
M 最終的な応答で途中の状態を置き換える。黒板でも、文字の追加から確定へ進むぜ。

## 04 ツール実行の往復 | 1ターンの境界
R モデルの返答に、ファイルを読むという指定が入っていたら？
M assistantの内容からtoolCallを取り出し、対応するツールを探して実行する。
M 結果はtoolResultのメッセージになり、会話の文脈へ追加されるんだ。
R その結果を読んだモデルが、次の返答を作るわけね。
M そうだ。この実装では、モデルの応答と、その応答が要求したツールの処理で1ターンだ。
M ツールが呼ばれたなら、通常は次のターンに進み、モデルをもう一度呼ぶ。
R だから、ツールの結果を表示しただけで依頼が終了とは限らないのね。
M 呼び出し、実行、結果の追加、再判断。この円をたどると基本ループが読めるぜ。

## 04 ツール実行の往復 | 実行前の検査
R モデルが出した引数は、そのままツールへ渡していいの？
M 実装は順番に確かめる。ツール名を探し、必要なら引数を整え、スキーマを検証する。
M その後でbeforeToolCallが呼ばれ、実行を止める判断もできる。
R 型の検査と、実行を許可する判断は別の段階なのね。
M そうだ。名前が存在しない場合や引数が不正な場合は、エラーの結果として扱う。
M 黒板の赤い経路は、実行本体へ進まず、結果の側へ戻る道を示している。
R モデルが書いた内容を信じて、何でも直接呼び出すわけではないのね。
M ただし、この仕組みだけで用途ごとの権限が自動決定するわけではない。判断は実装するんだ。

## 04 ツール実行の往復 | 実行後と失敗の扱い
R ツールの処理が終わった後にも、手を入れられる場所はあるの？
M afterToolCallがあり、最終的な結果をイベントや履歴に流す前に扱える。
M ツールの進捗は途中の更新、最終的な成功や失敗は終了のイベントとして伝わる。
R ファイルがなかった場合も、失敗を伝えて次の判断へ進めるのね。
M もう1つ大事なのは、モデルの出力が長さの上限で切れた場合の扱いだ。
M その応答にあるツール要求は、不完全な可能性があるため実行せず、失敗の結果にする。
R 途中まで届いた引数で、うっかり操作することを避けているのね。
M 成功だけでなく、どこで失敗が記録されるかを見ると、ループの責任が分かるぜ。

## 05 並列実行と順序 | 2つのツールを同時に動かす
R モデルが1回の応答で、2つのファイルを読むように頼んだら？
M この版では並列実行が既定だ。ただし、すぐに全部を走らせるわけではない。
M まず、それぞれの呼び出しについて、引数の検証や事前フックを順に処理する。
R 黒板では、まず2枚のカードが検査の門を通っているわね。
M その後で、実行可能な処理をまとめて並列に動かす。ここでPromise.allを使う。
M この例では読み取りAが遅く、読み取りBが先に終わるように描いている。
R 実際の速度を測った図ではなく、順序の違いを理解する例なのね。
M そうだ。同時に始めても、同時に終わるとは限らない点が重要なんだぜ。

## 05 並列実行と順序 | 完了順と履歴順は違う
R Bが先に終わったら、結果もB、Aの順でモデルへ渡すの？
M そこが面白い。終了イベントは完了に合わせて出るが、結果メッセージは元の呼び出し順だ。
M 黒板の上段はB、Aの完了通知。下段ではA、Bの順に履歴へ並び直している。
R 画面の進捗表示と、モデルが次に読む会話では、順序の意味が違うのね。
M 呼び出しの識別子も結果に付く。どの要求に対する返事かを対応づけるんだ。
M 並列部分だけを見て、履歴も完了順だと決めつけると、この挙動を読み違える。
R 結果の配列をどこで保存し、どこでメッセージにするかまで追う必要があるのね。
M イベントの観測順と、会話の論理的な順序を分ける設計として覚えておこう。

## 05 並列実行と順序 | 直列にする条件
R 何でも並列にすれば、速くて便利というわけではないわよね。
M 例えば、同じファイルを順番に変更する処理では、実行順が意味を持つことがある。
M 設定でsequentialを選ぶか、呼び出すツールのどれかが直列を要求すると、バッチ全体が直列になる。
R そのツールだけ別枠にするのではなく、まとまり全体の順序を守るのね。
M 黒板では、並んでいた2本の道が、Aの次にBが続く1本の道へ変わる。
M 直列なら、先の処理を終えてから次へ進む。並列なら、独立した待ち時間を重ねられる。
R どちらが正しいかは、ツール同士の関係によって変わるのね。
M 速度だけでなく、副作用と依存関係を見て選ぶ。ライブラリはその切り替えを用意しているぜ。

## 06 途中の指示と停止 | steerはどこへ入るのか
R 最初の依頼の途中で、やっぱり比較表にして、と補足したいわ。
M steerで追加すると、ステアリング用の待ち行列へ入る。進行中のループが取り出すんだ。
M 基本ループでは、ターンの境界などで待っているメッセージを確認する。
R 入力した瞬間に、実行中のツールが強制終了するわけではないのね。
M そうだ。黒板でも、処理中の点を消さずに、次の判断の手前へ補足を差し込んでいる。
M メッセージを取り出す量は、1件ずつか、まとめてかを設定できる。
R 画面の入力欄は同じでも、中では新規開始と補足を使い分ける必要があるわね。
M いまの仕事を方向修正する入口として読むと、待ち行列の存在に納得できるぜ。

## 06 途中の指示と停止 | followUpは仕事の続き
R followUpも追加指示なら、steerと同じタイミングで入るの？
M 違う。基本ループでは、ツールやステアリングの処理がなくなり、止まろうとするときに確認する。
M 例えば、比較が終わったら短い要約も作って、という次の仕事を待たせるイメージだ。
R 今の判断を変える指示と、一区切りついた後の仕事を分けるのね。
M 黒板では、ステアリングは円の途中、フォローアップは出口の先から戻ってくる。
M 待ち行列にメッセージがあれば、それを追加して処理を続ける。
R ツール呼び出しがなくなっただけでは、全体が終わるとは限らないわけね。
M 内側の反復と外側の反復を見ると、この2種類の追加指示をコードで追いやすくなるぜ。

## 06 途中の指示と停止 | 止める方法も複数ある
R 終わるまで待てないときは、どうやって中断するの？
M abortはAbortControllerを通して中止を知らせる。受け取る処理が信号に対応する仕組みだ。
M すでに終わった書き込みまで、自動で巻き戻す機能だとは考えないようにしよう。
R 今のターンを終えてから、穏やかに止める方法もあるのかしら。
M shouldStopAfterTurnがそれだ。ターンの終了後に判定し、次の呼び出しへ進まず終了できる。
M また、ツール結果のterminateで終える経路は、確定した結果の全件が終了を示す場合だ。
R 1件だけ終了を希望しても、ほかの結果がそうでなければ同じ扱いにはならないのね。
M 中止の信号、ターン後の判断、バッチ結果の判断。止まる位置を区別しておこう。

## 07 セッションを支える実行基盤 | AgentHarnessを重ねる
R ここまでのAgentで、長い作業の保存や再開まで全部できるの？
M 基本の会話状態と、保存を伴う実行基盤は分けて考えよう。ここでAgentHarnessが登場する。
M この版では、セッションとモデル管理を渡し、名前付きのlaneを通して作業を進められる。
R laneには、依頼や追加指示のほかに、実行状況を調べる入口もあるのね。
M さらに、文脈の圧縮や履歴上の移動、待ち行列の操作などが公開されている。
M Harness自体が1つのlaneなのではなく、複数のlaneを管理する立場だ。
R 黒板では外側の箱が管理役で、中の道がそれぞれの作業の入口になっているわ。
M ただし、道が複数あるだけで万能な協調が生まれるわけではない。役割分担はアプリが決めるんだ。

## 07 セッションを支える実行基盤 | 履歴は木として持てる
R セッションの履歴も、普通の会話のような1列の配列なのかしら。
M エントリには識別子と親の識別子がある。枝分かれした履歴を表せる構造だ。
M ブランチは先端を指し、そこから親をたどることで、その枝の流れを見つけられる。
R 同じ途中地点から、別の進め方を試す場合に役立ちそうね。
M 黒板では共通の2つの記録から、AとBの道へ枝分かれさせている。
M メッセージだけでなく、圧縮や枝の要約、独自の記録もエントリの種類に含まれる。
R 画面に出すチャットだけでなく、作業の経過を支える情報も持つのね。
M 保存の形を読むと、単純なメッセージ配列だけでは足りない理由が見えてくるぜ。

## 07 セッションを支える実行基盤 | 圧縮と保存先と実行環境
R 長い会話で、モデルに渡せる量を超えそうになったらどうするの？
M 圧縮の仕組みがあり、要約と残しておく末尾のメッセージを持つ記録が定義されている。
M 全記録を同じ量のまま毎回送るのではなく、次の判断に使う文脈を組み直すんだ。
R その記録は、必ずSQLiteへ自動保存されるのかしら。
M 保存の抽象化と具体的なバックエンドは別だ。SQLite用の実装は別パッケージとして案内されている。
M さらに、この版には読み書きやシェルのツールと、Node.jsの実行環境の実装も含まれる。
R モデル、会話制御、保存先、実行環境を、それぞれつなぐ必要があるのね。
M どの層を選んだかで必要な準備が変わる。小さなAgentの例と同じ条件だと思わないことだぜ。

## 08 読み方と検証 | ソースを読む順番
R 自分でもコードを読むなら、どこから戻って確認すればいい？
M まずREADMEで使い方を見て、agent.tsで入力と状態、agent-loop.tsで反復を追うといい。
M 型の意味で迷ったらtypes.tsへ戻る。その後で、関心のあるHarnessの処理へ進もう。
R 今回の2つのファイルを読む例なら、ツール結果が戻る場所を探せばよいのね。
M そうだ。次に、失敗した場合や、途中で補足した場合を同じ経路に重ねて考える。
M 正常系だけでなく、終了条件と順序が変わる場所を確認するのが効果的だ。
R 黒板の道筋を持ってから読むと、長いファイルでも探す場所を絞れそうね。
M 全行を同じ速さで読むより、具体的な疑問を持って、入口から出口までつなごう。

## 08 読み方と検証 | 何を実行確認したか
R 動画の説明は、実際のモデルへ依頼して確認した結果なの？
M 今回の説明の根拠は、固定したソースの静的な確認だ。外部モデルとの実行結果とは区別しよう。
M 上流の基本ループとAgentのテストも起動したが、依存データなどの不足で実行前に止まった。
R それを、テストが全部成功したとは言えないわね。
M その通り。テストには模擬の応答を返す仕組みがあり、通信と制御を分けて確認する狙いが読める。
M ただし、この環境で成功した証拠としては扱わない。実行コマンドと失敗理由は資料に残す。
R 黒板のファイル読み取りも、実際のモデルの記録ではなく、処理を説明するための例なのね。
M 動画の音声と映像が正しく出力できたかは、別に検証する。確認した範囲を混ぜないことが大切だぜ。

## 08 読み方と検証 | エージェントを組み立てる視点
R 最後に、agent-coreを読むことで何が分かったか整理したいわ。
M モデルの出力を受け取り、ツールを動かし、結果を文脈に戻す反復が中心にある。
M その周りに、途中経過の通知、追加指示、停止、保存を伴う実行基盤が重なっている。
R 並列で動いていても、会話の順序は別に整えている点が印象に残ったわ。
M そして、入力を受ける場所と、次の判断へ進む境界を分けると、挙動を予測しやすくなる。
M 自作するときも、画面、モデル通信、会話制御、保存先をどこでつなぐか考えてみよう。
R 便利な関数の一覧としてではなく、仕事を進める流れとして理解できた気がするわ。
M 次は1つの依頼を選んで、イベントと履歴を並べて追ってみてくれ。それが実装を読むよい練習になるぜ。
