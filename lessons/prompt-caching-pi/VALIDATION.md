# 検証記録

2026-09-08。台本JSONの作成と静的解析まで。全編動画・合成音声・Piの実APIベンチマークは未生成／未実行。

- 制作ブリーフ `d77e96ca62e94df8bb9777efb7305ef3` を読み取り、原本を変更せず `productionBrief` に採用条件と出典を保持した。
- `targetMinutes: 20` はトップレベル。10章の計画は導入・まとめ・問いかけの間を含め1200秒。文字数6189、180セリフの推定は1231秒（20分31秒）。音声未合成のため完成尺ではない。
- `presentationMode: dialogue`、2話者、音声・画像・字幕色を保存した。各セリフに `speech`、`boardStep`、`boardFocus`、話速1.0、間、表情・ポーズを設定。図の実際の進行は `boardAnimation` が優先する。
- 30場面すべてでアニメーションと字幕配列の一致、フレーム番号、座標の範囲、要素数、文字量を検証した。
- 全180字幕の2行枠内への収まりを確認した。各セリフの開始・50%・99%を描画し、540枚のフレームがエラーなく生成された。
- 30場面の終端フレームをコンタクトシートで目視。APIの分岐と比較の矢印を修正し、追加をツール置換と誤解させない表記に調整した。プレビューの開始・途中・終端も抽出した。
- `server.import_script` → `server.script` の往復で全フィールド一致。新しいUUIDの編集用コピーへ保存。既存台本・制作ブリーフ・編集データは未変更。
- Piは `b2602be77cb7b0de45dd616407fd210daa48aa75` に固定。全22個のコード参照が実在するファイルと行範囲を指すことを確認した。READMEだけでなく送信変換、ツール結果のラッパー、履歴構成、統計、対応テストを静的に読んだ。Piの依存パッケージは導入せず、対象コード・対象テストは未実行。
- 仮の料金例は100+100=200、125+10=135、200−135=65、125+125=250を検算。料金表の引用や実測ではない。
- `output/prompt-caching-pi-20260908` に各30秒、1280×720、12fpsのH.264 MP4を2本生成し、同梱FFmpegで全編デコードが成功。音声トラックはない。各セリフ5秒の絵コンテ確認用で、完成音声のタイミングとは異なる。
- Macのフォントパスと実パス比較を修正し、既存のPython検証24件が成功した。Windows側の既定フォントは維持。Windows実機での再確認はしていない。

## 未確認事項

VOICEVOXが見つからないため、合成音声の発音・話者スタイルの可用性・音声からの完成尺・口パクは未確認。語句やバージョンの読みは `speech` で調整したが、実際の発音は音声生成後に確認する。全編動画生成時には、その時点で保存された編集用スナップショットを起点にし、変更された字幕に合わせて黒板演出も再調整する。
