fn add(a : Int, b : Int) -> Int { a + b }
fn or_zero(x : Int?) -> Int { match x { Some(n) => n; None => 0 } }
test "addition and fallback" {
  assert_eq(add(6, 7), 13)
  assert_eq(or_zero(None), 0)
  assert_eq(or_zero(Some(9)), 9)
}
fn main { println(add(6, 7)) }
